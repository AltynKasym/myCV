// import { useState } from "react";

// export const winQueue = [false, false, false, false, false];
// export const [active, setActive] = useState(winQueue);

// export function activeWindow(id) {
//     winQueue.forEach((item, ind) => {
//         id === ind ? (winQueue[ind] = true) : (winQueue[ind] = false);
//     });

//     setActive(winQueue);
// }
